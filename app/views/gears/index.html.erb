<h1>ALL GEARS</h1>
<ul>
<% @gears.each do |gear| %>
<li>
<a href="<%= gear_path(gear.id) %>">
        see more
        </a>
<%= gear.gear_type %>
<%= gear.summary %>
<%= gear.price %>
<%= link_to "book this gear", new_gear_rental_path(gear) %>
</li>
<% end %>
</ul>
